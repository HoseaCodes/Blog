SKIP_PREFLIGHT_CHECK=
MONGODB_URL=
FOREMAPI=
MEDIUMAPI=
MEDIUMUSER=
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=
CLOUD_API_KEY=
CLOUD_API_SECRET=
CLOUND_NAME=
OPENAI_API_KEY=

# AI art (Phase 1)
STABILITY_API_KEY=
AI_ART_PNG_PRICE_USD=5

# PayPal (set to "live" only for production; defaults to sandbox)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_ENV=sandbox

# Newsletter (Resend). RESEND_FROM must be at a domain verified in your Resend
# dashboard. Without RESEND_API_KEY, signup still creates rows but verify and
# broadcast emails are no-ops (logged as warnings).
RESEND_API_KEY=
RESEND_FROM="Dominique Hosea <newsletter@hoseacodes.com>"
SITE_URL=https://hoseacodes.com